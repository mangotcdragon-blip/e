package com.dailytools.calculator.ui.theme

import androidx.compose.ui.graphics.Color

// Calculator (disguise) palette — kept neutral/light so it reads as an ordinary utility app.
val CalcBackground = Color(0xFFF5F5F5)
val CalcDisplayText = Color(0xFF212121)
val CalcNumberKey = Color(0xFFFFFFFF)
val CalcNumberKeyText = Color(0xFF212121)
val CalcFunctionKey = Color(0xFFE0E0E0)
val CalcFunctionKeyText = Color(0xFF212121)
val CalcOperatorKey = Color(0xFFFF9800)
val CalcOperatorKeyText = Color(0xFFFFFFFF)

// Gallery app palette (dark by default).
val GalleryBackgroundDark = Color(0xFF121212)
val GallerySurfaceDark = Color(0xFF1E1E1E)
val GalleryPrimary = Color(0xFFFF5C8A)
val GalleryOnPrimary = Color(0xFF3A0018)
val GalleryBackgroundLight = Color(0xFFFFFBFE)
val GallerySurfaceLight = Color(0xFFF3F0F3)
